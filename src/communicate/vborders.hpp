#ifndef _VBORDERS_HPP
#define _VBORDERS_HPP

#include "geometry/geometry_vir.hpp"

namespace nissa
{
  template <class base_type> template <class T> void vranks_ord_t<base_type>::communicate_vborders(T *v)
  {
    qua bisogna scrivere le routine che per ogni direzione (e quindi, per ogni bordo) capiscono come si devono fare le permutazioni 
    vg->nvranks_per_dir;
  }
}

#endif
